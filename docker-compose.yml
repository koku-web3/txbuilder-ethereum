services:
  txbuilder-ethereum:
    container_name: txbuilder-ethereum
    build:
      context: .
      dockerfile: Dockerfile
    image: txbuilder-ethereum:latest
    ports:
      - "51051:51051"
    volumes:
      - ./config:/app/config:rw
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "51051"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - txbuilder-network

networks:
  txbuilder-network:
    driver: bridge
